














Your task is to implement a fucntion, handleJSON, which receives a JSON string as it s first argument and a saveToFile function as its formmated data to a file .
Requirements 
The provided JSON will always contain username and filename properties and may contain other properties. both username and filename are always strings.
the followin steps should be performed by the handleJSON function 

1. Normalize username and filename: 
    1. change accented characters to thier unaccented ASCII equaivalents.
    2. remove whitespaces from both ends of any string;
    3. replace all remaining spaces with _;